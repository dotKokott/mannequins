{
  "name": "mannequins",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun build ./src/index.ts --outdir ./out --watch",
    "serve": "cd out && python -m http.server 9000"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "openai": "^4.30.0"
  }
}